### CODE BLOCK 1
```

Hi {{FirstName}},

```

### CODE BLOCK 2
```

We wanted to personally let you know about something new we’ve launched for our community.

```

### CODE BLOCK 3
```

Here’s what this means for you:

• Faster access to updates  
• Priority notifications  
• Special subscriber-only offers  

We built this specifically with our customers in mind.

```

### CODE BLOCK 4
```

You can explore it here:
[Explore Now Button]

```

### CODE BLOCK 5
```

As always, thank you for being part of our community.

```

### CODE BLOCK 6
```

{{Business Name}}
{{Physical Address}}

If you prefer not to receive promotional emails, you can unsubscribe here.

```

### CODE BLOCK 7
```

mail.yourdomain.com
notify.yourdomain.com
clients.yourdomain.com

```

### CODE BLOCK 8
```

Hi [FirstName],

I noticed your business [Company] is actively engaging with your clients via email. I wanted to reach out because we help companies like yours ensure their emails land directly in the inbox — every time.  

We’re offering a free Email Deliverability Audit to identify gaps and optimize your campaigns.  

Would you like me to send over the audit so you can see your current inbox performance?  

Best regards,  
[Your Name]

```

### CODE BLOCK 9
```

Hi [FirstName],

Many businesses lose revenue because their emails land in spam or promotions.  

We provide a compliance-first email infrastructure that guarantees high deliverability and actionable analytics.  

I’d love to provide you with a free Deliverability Audit for [Company] — it only takes a few minutes and highlights opportunities for improvement.  

Interested?  
[Your Name]

```

### CODE BLOCK 10
```

Hi [FirstName],

Quick question: Are your marketing emails actually reaching your subscribers’ inboxes?  

We run a free Email Deliverability Audit for companies like [Company] to uncover hidden issues and increase campaign effectiveness.  

Would you like me to set one up for you this week?  

Cheers,  
[Your Name]

```

### CODE BLOCK 11
```

Subject: Are your emails landing in the inbox?

Hi [FirstName],

I’m reaching out because many businesses aren’t realizing their email campaigns are ending up in spam or promotions folders.  

We run a Free Email Deliverability Audit for companies like [Company], showing exactly how to improve inbox placement and engagement.  

Would you like me to schedule a quick audit for your team?  

Best,  
[Your Name]

```

### CODE BLOCK 12
```

Subject: Maximize your email performance

Hi [FirstName],

Imagine every email you send actually reaches your subscriber’s inbox.  

Our platform provides high-deliverability email infrastructure and actionable insights. I’d like to offer [Company] a complimentary Deliverability Audit to identify improvement areas.  

Can we schedule a 10-minute session this week?  

Thanks,  
[Your Name]

```

### CODE BLOCK 13
```

Subject: How top SMEs are improving inbox delivery

Hi [FirstName],

Several businesses similar to [Company] have improved open rates by 20–30% using our compliance-first email platform.  

We offer a Free Email Deliverability Audit so you can see your current email performance and opportunities.  

Would you like me to set it up for your team?  

Regards,  
[Your Name]

```

### CODE BLOCK 14
```

Hi [FirstName], this is [Your Name]. 👋  

We help businesses like [Company] make sure their emails actually reach the inbox.  

I’d like to offer a free Email Deliverability Audit — quick, actionable, and no strings attached.  

Do you want me to send it over?

```

### CODE BLOCK 15
```

Hello [FirstName], [Your Name] here.  

Many companies are missing out on revenue because emails land in spam. We provide a compliance-first platform and can run a free audit for [Company] to identify improvement areas.  

Can we set it up this week?  

```

### CODE BLOCK 16
```

Hi [PartnerName],  

We’re offering a premium Email Deliverability Platform for SMEs. I’d like to invite you to join our partner program — refer businesses to us and earn [commission] per paying tenant.  

Your clients get a free Deliverability Audit, and you earn when they sign up. Interested?  

```

### CODE BLOCK 17
```

Hello [PartnerName],  

We’re building a high-end email infrastructure platform and looking for partners to help refer businesses.  

Each referred client receives a free Email Deliverability Audit, and you earn a commission when they become paying tenants.  

Would you like to join the program?  

```

### CODE BLOCK 18
```

CRM Lead: Test Lead | Status: New | Notes: \\\"\\\"
Demo Tenant Created: TestCo
Email Sequence: 3 emails scheduled → Inbox test@example.com

```

### CODE BLOCK 19
```

Email 1 received: Welcome + Audit instructions
Email 2 received: Tips for Inbox Deliverability
Email 3 received: CTA to schedule Demo
Click tracking active

```

### CODE BLOCK 20
```

New → Contacted → Interested → Demo Booked → Closed
Add notes: \\\"Test notes at each stage\\\"

```

### CODE BLOCK 21
```

Total Leads: 1
Demo Booked: 1
Conversion Rate: 100%
MRR Placeholder: Ready

```

### CODE BLOCK 22
```

Partner: Test Partner
Lead: Partner Lead | Status: New
Commission placeholder: 0 (manual payout ready)

```

### CODE BLOCK 23
```

Total Leads: 2
Demo Booked: 1–2
Conversion Rate: 50–100% (simulated)
MRR: Placeholder updated

```

### CODE BLOCK 24
```

https://projectname.lovable.app/test-run

```

### CODE BLOCK 25
```

     https://<projectname>.lovable.app/lead-test

```

### CODE BLOCK 26
```

     https://<projectname>.lovable.app/lead-test?name=Test%20Lead&email=test%40example.com&company=TestCo&volume=1000

```

### CODE BLOCK 27
```

https://yourcompany.com

```

### CODE BLOCK 28
```

https://www.autoswift.com

```

### CODE BLOCK 29
```

https://www.autoswift.com/lead-capture

```

### CODE BLOCK 30
```

Hi [FirstName],

I noticed [Company] is actively engaging with clients via email. At Autoswift, we help businesses ensure emails reach the inbox every time.

We offer a free Email Deliverability Audit to uncover any issues and optimize campaigns.

Would you like me to send over the audit so you can see your current inbox performance?

Best regards,
[Your Name] | Autoswift

```

### CODE BLOCK 31
```

Hi [FirstName],

Many companies lose revenue because emails land in spam. Autoswift provides compliance-first email infrastructure for maximum deliverability.

I’d like to offer [Company] a free Deliverability Audit — quick and actionable.

Interested?

```

### CODE BLOCK 32
```

Hi [FirstName],

Quick question: Are your marketing emails actually reaching your subscribers’ inboxes?

Autoswift provides a free Deliverability Audit for companies like [Company]. It highlights opportunities to improve inbox performance.

Would you like me to set one up this week?

```

### CODE BLOCK 33
```

Subject: Are your emails landing in the inbox?

Hi [FirstName],

Many businesses don’t realize their emails are ending up in spam. Autoswift runs a Free Email Deliverability Audit to show exactly how to improve placement and engagement.

Would you like me to schedule a quick audit for your team?

Best,  
[Your Name]

```

### CODE BLOCK 34
```

Subject: Maximize your email performance

Hi [FirstName],

Imagine every email you send actually reaching your subscriber’s inbox. Autoswift ensures high deliverability and actionable insights.

I’d like to offer [Company] a complimentary Deliverability Audit to identify improvement areas.

Can we schedule a 10-minute session this week?

```

### CODE BLOCK 35
```

Subject: How top SMEs are improving inbox delivery

Hi [FirstName],

Several businesses similar to [Company] have improved open rates by 20–30% using Autoswift.  

We offer a Free Email Deliverability Audit to see your current performance.

Would you like me to set it up for your team?

```

### CODE BLOCK 36
```

Hi [FirstName], this is [Your Name] from Autoswift 👋

We help businesses like [Company] make sure emails actually reach the inbox.

I’d like to offer a free Email Deliverability Audit — quick, actionable, and no strings attached.  

Do you want me to send it over?

```

### CODE BLOCK 37
```

Hello [FirstName], [Your Name] here at Autoswift.

Many companies miss revenue because emails land in spam. We provide a compliance-first platform and can run a free audit for [Company].

Can we set it up this week?

```

### CODE BLOCK 38
```

Hi [PartnerName],

Autoswift offers a premium Email Deliverability Platform for SMEs. Join our partner program — refer businesses and earn [commission] per paying tenant.

Your clients get a free Deliverability Audit, and you earn when they sign up. Interested?

```

### CODE BLOCK 39
```

Hello [PartnerName],

We’re building a high-end email infrastructure platform and looking for partners.  

Each referred client receives a free Deliverability Audit, and you earn commission when they become paying tenants.  

Would you like to join the program?

```

### CODE BLOCK 40
```

https://www.autoswiftco.com/lead-capture

```

### CODE BLOCK 41
```

https://autoswiftco.com

```

### CODE BLOCK 42
```

autoswift

```

### CODE BLOCK 43
```

autoswift/
│
├── package.json
├── next.config.js
├── pages/
│   ├── index.js
│   └── api/
│       └── lead.js
├── styles/
│   └── globals.css

```

### CODE BLOCK 44
```
json
{
  \\\"name\\\": \\\"autoswift\\\",
  \\\"version\\\": \\\"1.0.0\\\",
  \\\"private\\\": true,
  \\\"scripts\\\": {
    \\\"dev\\\": \\\"next dev\\\",
    \\\"build\\\": \\\"next build\\\",
    \\\"start\\\": \\\"next start\\\"
  },
  \\\"dependencies\\\": {
    \\\"next\\\": \\\"14.0.0\\\",
    \\\"react\\\": \\\"18.2.0\\\",
    \\\"react-dom\\\": \\\"18.2.0\\\"
  }
}

```

### CODE BLOCK 45
```
javascript
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

module.exports = nextConfig;

```

### CODE BLOCK 46
```
css
body {
  margin: 0;
  font-family: Arial, sans-serif;
  background-color: #0a0f1c;
  color: white;
}

.container {
  max-width: 800px;
  margin: auto;
  padding: 40px 20px;
}

h1 {
  font-size: 3rem;
  margin-bottom: 10px;
}

.highlight {
  color: #00d4ff;
}

form {
  margin-top: 30px;
  display: flex;
  flex-direction: column;
  gap: 15px;
}

input, button {
  padding: 12px;
  border-radius: 6px;
  border: none;
}

input {
  font-size: 16px;
}

button {
  background-color: #00d4ff;
  font-weight: bold;
  cursor: pointer;
}

```

### CODE BLOCK 47
```
javascript
import { useState } from \\\"react\\\";

export default function Home() {
  const [status, setStatus] = useState(\\\"\\\");

  async function handleSubmit(e) {
    e.preventDefault();

    const formData = {
      name: e.target.name.value,
      email: e.target.email.value,
      company: e.target.company.value,
      volume: e.target.volume.value
    };

    const res = await fetch(\\\"/api/lead\\\", {
      method: \\\"POST\\\",
      headers: { \\\"Content-Type\\\": \\\"application/json\\\" },
      body: JSON.stringify(formData),
    });

    if (res.ok) {
      setStatus(\\\"Lead submitted successfully 🚀\\\");
      e.target.reset();
    } else {
      setStatus(\\\"Something went wrong.\\\");
    }
  }

  return (
    <div className=\\\"container\\\">
      <h1>
        Welcome to <span className=\\\"highlight\\\">Autoswift</span>
      </h1>
      <p>
        Premium Email Deliverability Infrastructure for Modern Businesses.
      </p>

      <form onSubmit={handleSubmit}>
        <input name=\\\"name\\\" placeholder=\\\"Full Name\\\" required />
        <input name=\\\"email\\\" type=\\\"email\\\" placeholder=\\\"Email Address\\\" required />
        <input name=\\\"company\\\" placeholder=\\\"Company Name\\\" required />
        <input name=\\\"volume\\\" placeholder=\\\"Monthly Email Volume\\\" required />
        <button type=\\\"submit\\\">Request Free Deliverability Audit</button>
      </form>

      {status && <p>{status}</p>}
    </div>
  );
}

```

### CODE BLOCK 48
```
javascript
export default async function handler(req, res) {
  if (req.method !== \\\"POST\\\") {
    return res.status(405).json({ message: \\\"Method not allowed\\\" });
  }

  const { name, email, company, volume } = req.body;

  console.log(\\\"New Lead:\\\", {
    name,
    email,
    company,
    volume,
    createdAt: new Date()
  });

  // Here later you can:
  // - Save to database
  // - Send email via SendGrid / SES
  // - Push to CRM

  return res.status(200).json({ message: \\\"Lead captured successfully\\\" });
}

```

### CODE BLOCK 49
```

git init
git add .
git commit -m \\\"Autoswift v1 production build\\\"

```

### CODE BLOCK 50
```

git remote add origin https://github.com/YOURUSERNAME/autoswift.git
git branch -M main
git push -u origin main

```

### CODE BLOCK 51
```

https://autoswift.vercel.app

```

### CODE BLOCK 52
```
sql
User
- id
- name
- email
- password
- role (USER | ADMIN)
- subscriptionStatus
- createdAt

Company
- id
- name
- ownerId
- createdAt

Vehicle
- id
- companyId
- customerName
- carModel
- status
- amount
- createdAt

Subscription
- id
- userId
- stripeCustomerId
- stripeSubscriptionId
- plan
- status

```

### CODE BLOCK 53
```
ts
if (session.user.role !== \\\"ADMIN\\\") {
  return redirect(\\\"/\\\")
}

```

### CODE BLOCK 54
```

/admin
/admin/users
/admin/subscriptions
/admin/analytics

```

### CODE BLOCK 55
```

/api/webhooks/stripe

```

### CODE BLOCK 56
```
ts
await sendgrid.send({
  to: user.email,
  from: \\\"support@autoswift.com\\\",
  subject: \\\"Welcome to AutoSwift\\\",
  html: \\\"<strong>Your account is ready</strong>\\\"
})

```

### CODE BLOCK 57
```
ts
where: {
  companyId: session.user.companyId
}

```

### CODE BLOCK 58
```

/app
  /dashboard
  /admin
  /billing
/api
  /auth
  /stripe
  /webhooks
/prisma
  schema.prisma

```

### CODE BLOCK 59
```

autoswift/
│
├── app/
│   ├── layout.tsx
│   ├── page.tsx
│   ├── dashboard/page.tsx
│   ├── admin/page.tsx
│   ├── billing/page.tsx
│
├── app/api/
│   ├── auth/[...nextauth]/route.ts
│   ├── stripe/checkout/route.ts
│   ├── webhooks/stripe/route.ts
│
├── lib/
│   ├── prisma.ts
│   ├── stripe.ts
│   ├── sendgrid.ts
│   ├── auth.ts
│
├── prisma/
│   └── schema.prisma
│
├── middleware.ts
├── .env.example
├── package.json
└── next.config.js

```

### CODE BLOCK 60
```
prisma
generator client {
  provider = \\\"prisma-client-js\\\"
}

datasource db {
  provider = \\\"postgresql\\\"
  url      = env(\\\"DATABASE_URL\\\")
}

model User {
  id                  String   @id @default(uuid())
  name                String?
  email               String   @unique
  password            String
  role                Role     @default(USER)
  company             Company? @relation(fields: [companyId], references: [id])
  companyId           String?
  stripeCustomerId    String?
  subscriptionStatus  String?
  createdAt           DateTime @default(now())
}

model Company {
  id        String   @id @default(uuid())
  name      String
  ownerId   String
  users     User[]
  vehicles  Vehicle[]
  createdAt DateTime @default(now())
}

model Vehicle {
  id          String   @id @default(uuid())
  company     Company  @relation(fields: [companyId], references: [id])
  companyId   String
  customerName String
  carModel     String
  status       String
  amount       Float
  createdAt    DateTime @default(now())
}

enum Role {
  USER
  ADMIN
}

```

### CODE BLOCK 61
```
bash
npx prisma generate
npx prisma migrate dev --name init

```

### CODE BLOCK 62
```
ts
import NextAuth from \\\"next-auth\\\"
import CredentialsProvider from \\\"next-auth/providers/credentials\\\"
import { PrismaAdapter } from \\\"@next-auth/prisma-adapter\\\"
import { prisma } from \\\"@/lib/prisma\\\"
import bcrypt from \\\"bcrypt\\\"

export const authOptions = {
  adapter: PrismaAdapter(prisma),
  providers: [
    CredentialsProvider({
      name: \\\"Credentials\\\",
      credentials: {
        email: {},
        password: {}
      },
      async authorize(credentials) {
        const user = await prisma.user.findUnique({
          where: { email: credentials.email }
        })

        if (!user) return null

        const valid = await bcrypt.compare(
          credentials.password,
          user.password
        )

        if (!valid) return null

        return user
      }
    })
  ],
  session: { strategy: \\\"jwt\\\" }
}

const handler = NextAuth(authOptions)
export { handler as GET, handler as POST }

```

### CODE BLOCK 63
```
ts
import Stripe from \\\"stripe\\\"

export const stripe = new Stripe(process.env.STRIPE_SECRET_KEY!, {
  apiVersion: \\\"2023-10-16\\\"
})

```

### CODE BLOCK 64
```
ts
import { stripe } from \\\"@/lib/stripe\\\"

export async function POST(req: Request) {
  const session = await stripe.checkout.sessions.create({
    payment_method_types: [\\\"card\\\"],
    mode: \\\"subscription\\\",
    line_items: [
      {
        price: process.env.STRIPE_PRICE_ID,
        quantity: 1
      }
    ],
    success_url: `${process.env.NEXT_PUBLIC_APP_URL}/dashboard`,
    cancel_url: `${process.env.NEXT_PUBLIC_APP_URL}/billing`
  })

  return Response.json({ url: session.url })
}

```

### CODE BLOCK 65
```
ts
import sgMail from \\\"@sendgrid/mail\\\"

sgMail.setApiKey(process.env.SENDGRID_API_KEY!)

export async function sendEmail(to: string, subject: string, html: string) {
  await sgMail.send({
    to,
    from: \\\"support@autoswift.com\\\",
    subject,
    html
  })
}

```

### CODE BLOCK 66
```
ts
export { default } from \\\"next-auth/middleware\\\"

export const config = {
  matcher: [\\\"/dashboard/:path*\\\", \\\"/admin/:path*\\\"]
}

```

### CODE BLOCK 67
```
bash
git init
git remote add origin https://github.com/YOUR_USERNAME/autoswift.git
git add .
git commit -m \\\"Initial production commit\\\"
git push -u origin main

```

### CODE BLOCK 68
```

https://yourdomain.com/api/webhooks/stripe

```

### CODE BLOCK 69
```

DATABASE_URL=
NEXTAUTH_SECRET=
NEXTAUTH_URL=
STRIPE_SECRET_KEY=
STRIPE_PRICE_ID=
SENDGRID_API_KEY=
NEXT_PUBLIC_APP_URL=

```

### CODE BLOCK 70
```

autoswift.com

```

### CODE BLOCK 71
```
prisma
model EmailLog {
  id          String   @id @default(uuid())
  to          String
  subject     String
  status      String
  providerId  String?
  error       String?
  attempts    Int      @default(0)
  sentAt      DateTime?
  createdAt   DateTime @default(now())
}

model SuppressionList {
  id        String   @id @default(uuid())
  email     String   @unique
  reason    String
  createdAt DateTime @default(now())
}

```

### CODE BLOCK 72
```
ts
import sgMail from \\\"@sendgrid/mail\\\"
import { prisma } from \\\"@/lib/prisma\\\"

sgMail.setApiKey(process.env.SENDGRID_API_KEY!)

export async function sendTransactionalEmail({
  to,
  subject,
  html
}: {
  to: string
  subject: string
  html: string
}) {

  // 1️⃣ Check suppression list
  const suppressed = await prisma.suppressionList.findUnique({
    where: { email: to }
  })

  if (suppressed) {
    throw new Error(\\\"Email suppressed due to prior bounce/complaint\\\")
  }

  // 2️⃣ Create email log
  const log = await prisma.emailLog.create({
    data: {
      to,
      subject,
      status: \\\"PENDING\\\"
    }
  })

  try {
    const response = await sgMail.send({
      to,
      from: process.env.MAIL_FROM!,
      subject,
      html,
      trackingSettings: {
        clickTracking: { enable: true },
        openTracking: { enable: true }
      }
    })

    await prisma.emailLog.update({
      where: { id: log.id },
      data: {
        status: \\\"SENT\\\",
        providerId: response[0].headers[\\\"x-message-id\\\"],
        sentAt: new Date()
      }
    })

  } catch (error: any) {

    await prisma.emailLog.update({
      where: { id: log.id },
      data: {
        status: \\\"FAILED\\\",
        error: error.message,
        attempts: { increment: 1 }
      }
    })

    throw error
  }
}

```

### CODE BLOCK 73
```
ts
const failedEmails = await prisma.emailLog.findMany({
  where: {
    status: \\\"FAILED\\\",
    attempts: { lt: 3 }
  }
})

```

### CODE BLOCK 74
```

/api/webhooks/sendgrid

```

### CODE BLOCK 75
```
ts
export async function POST(req: Request) {
  const events = await req.json()

  for (const event of events) {

    if (event.event === \\\"bounce\\\" || event.event === \\\"spamreport\\\") {
      await prisma.suppressionList.create({
        data: {
          email: event.email,
          reason: event.event
        }
      })
    }
  }

  return new Response(\\\"ok\\\")
}

```

### CODE BLOCK 76
```

v=DMARC1; p=quarantine; rua=mailto:dmarc@autoswift.com

```

### CODE BLOCK 77
```
ts
text: \\\"Your AutoSwift account is ready.\\\"

```

### CODE BLOCK 78
```

SENDGRID_API_KEY=
MAIL_FROM=notifications@autoswift.com
SENDGRID_WEBHOOK_SECRET=

```

### CODE BLOCK 79
```

autoswiftmail.com
autoswiftapp.net
autoswiftcloud.com

```

### CODE BLOCK 80
```

tx.autoswiftmail.com       → Transactional
mkt.autoswiftmail.com      → Marketing
notify.autoswiftmail.com   → Financial alerts

```

### CODE BLOCK 81
```

v=spf1 include:sendgrid.net ~all

```

### CODE BLOCK 82
```

v=DMARC1; p=reject; rua=mailto:dmarc@autoswiftmail.com

```

### CODE BLOCK 83
```

v=DMARC1; p=quarantine; rua=mailto:dmarc@autoswiftmail.com

```

### CODE BLOCK 84
```
prisma
model EmailQueue {
  id        String   @id @default(uuid())
  to        String
  subject   String
  html      String
  type      EmailType
  status    String   @default(\\\"QUEUED\\\")
  attempts  Int      @default(0)
  createdAt DateTime @default(now())
}

enum EmailType {
  TRANSACTIONAL
  MARKETING
  FINANCE
}

```

### CODE BLOCK 85
```
ts
await sgMail.send(...)

```

### CODE BLOCK 86
```

/unsubscribe?email=abc@mail.com

```

### CODE BLOCK 87
```
ts
await prisma.suppressionList.create(...)

```

### CODE BLOCK 88
```

alerts@notify.autoswiftmail.com

```

### CODE BLOCK 89
```

gmail / yahoo / outlook

```

### CODE BLOCK 90
```

autoswiftmail.com

```

### CODE BLOCK 91
```

tx.autoswiftmail.com        → Transactional
notify.autoswiftmail.com    → Finance alerts
mkt.autoswiftmail.com       → Marketing

```

### CODE BLOCK 92
```

v=spf1 include:sendgrid.net -all

```

### CODE BLOCK 93
```

v=DMARC1; p=reject; rua=mailto:dmarc@autoswiftmail.com; adkim=s; aspf=s

```

### CODE BLOCK 94
```
prisma
model EmailQueue {
  id          String   @id @default(uuid())
  to          String
  subject     String
  html        String
  text        String
  type        EmailType
  status      String   @default(\\\"QUEUED\\\")
  attempts    Int      @default(0)
  scheduledAt DateTime @default(now())
  createdAt   DateTime @default(now())
}

model EmailLog {
  id          String   @id @default(uuid())
  emailQueue  EmailQueue @relation(fields: [queueId], references: [id])
  queueId     String
  providerId  String?
  event       String
  timestamp   DateTime @default(now())
}

model SuppressionList {
  id        String   @id @default(uuid())
  email     String   @unique
  reason    String
  createdAt DateTime @default(now())
}

enum EmailType {
  TRANSACTIONAL
  FINANCE
  MARKETING
}

```

### CODE BLOCK 95
```
ts
function resolveSender(type: EmailType) {
  switch (type) {
    case \\\"TRANSACTIONAL\\\":
      return \\\"no-reply@tx.autoswiftmail.com\\\"

    case \\\"FINANCE\\\":
      return \\\"alerts@notify.autoswiftmail.com\\\"

    case \\\"MARKETING\\\":
      return \\\"news@mkt.autoswiftmail.com\\\"
  }
}

```

### CODE BLOCK 96
```

/api/unsubscribe

```

### CODE BLOCK 97
```
prisma
model EmailQueue {
  id          String   @id @default(uuid())
  to          String
  subject     String
  html        String
  text        String
  type        EmailType
  status      String   @default(\\\"QUEUED\\\")
  attempts    Int      @default(0)
  lastAttempt DateTime?
  scheduledAt DateTime @default(now())
  createdAt   DateTime @default(now())
}

model EmailLog {
  id         String   @id @default(uuid())
  queueId    String
  providerId String?
  event      String
  metadata   Json?
  createdAt  DateTime @default(now())
}

model SuppressionList {
  id        String   @id @default(uuid())
  email     String   @unique
  reason    String
  createdAt DateTime @default(now())
}

enum EmailType {
  TRANSACTIONAL
  FINANCE
  MARKETING
}

```

### CODE BLOCK 98
```
ts
import { EmailType } from \\\"@prisma/client\\\"

export function resolveSender(type: EmailType) {
  switch (type) {
    case \\\"TRANSACTIONAL\\\":
      return \\\"no-reply@tx.autoswiftmail.com\\\"

    case \\\"FINANCE\\\":
      return \\\"alerts@notify.autoswiftmail.com\\\"

    case \\\"MARKETING\\\":
      return \\\"news@mkt.autoswiftmail.com\\\"
  }
}

```

### CODE BLOCK 99
```
ts
import { prisma } from \\\"@/lib/prisma\\\"
import sgMail from \\\"@sendgrid/mail\\\"
import { resolveSender } from \\\"@/lib/emailSenderResolver\\\"

sgMail.setApiKey(process.env.SENDGRID_API_KEY!)

export async function GET() {

  const batchLimit = 200 // adjustable via warmup strategy

  const emails = await prisma.emailQueue.findMany({
    where: {
      status: \\\"QUEUED\\\",
      attempts: { lt: 5 }
    },
    take: batchLimit
  })

  for (const email of emails) {

    const suppressed = await prisma.suppressionList.findUnique({
      where: { email: email.to }
    })

    if (suppressed) {
      await prisma.emailQueue.update({
        where: { id: email.id },
        data: { status: \\\"SUPPRESSED\\\" }
      })
      continue
    }

    try {
      const response = await sgMail.send({
        to: email.to,
        from: resolveSender(email.type),
        subject: email.subject,
        html: email.html,
        text: email.text
      })

      await prisma.emailQueue.update({
        where: { id: email.id },
        data: {
          status: \\\"SENT\\\",
          attempts: { increment: 1 },
          lastAttempt: new Date()
        }
      })

      await prisma.emailLog.create({
        data: {
          queueId: email.id,
          providerId: response[0].headers[\\\"x-message-id\\\"],
          event: \\\"SENT\\\"
        }
      })

    } catch (error: any) {

      await prisma.emailQueue.update({
        where: { id: email.id },
        data: {
          status: \\\"FAILED\\\",
          attempts: { increment: 1 },
          lastAttempt: new Date()
        }
      })

      await prisma.emailLog.create({
        data: {
          queueId: email.id,
          event: \\\"FAILED\\\",
          metadata: { message: error.message }
        }
      })
    }
  }

  return new Response(\\\"Worker Completed\\\")
}

```

### CODE BLOCK 100
```
json
{
  \\\"cron\\\": [
    {
      \\\"path\\\": \\\"/api/email/worker\\\",
      \\\"schedule\\\": \\\"*/1 * * * *\\\"
    }
  ]
}

```

### CODE BLOCK 101
```
ts
import { prisma } from \\\"@/lib/prisma\\\"

export async function POST(req: Request) {

  const events = await req.json()

  for (const event of events) {

    if ([\\\"bounce\\\", \\\"spamreport\\\"].includes(event.event)) {

      await prisma.suppressionList.upsert({
        where: { email: event.email },
        update: { reason: event.event },
        create: {
          email: event.email,
          reason: event.event
        }
      })
    }

    await prisma.emailLog.create({
      data: {
        queueId: event.sg_message_id || \\\"\\\",
        event: event.event,
        metadata: event
      }
    })
  }

  return new Response(\\\"OK\\\")
}

```

### CODE BLOCK 102
```
ts
const DAILY_LIMIT = 5000 // increase gradually

const todayCount = await prisma.emailQueue.count({
  where: {
    status: \\\"SENT\\\",
    createdAt: {
      gte: new Date(new Date().setHours(0,0,0,0))
    }
  }
})

if (todayCount >= DAILY_LIMIT) {
  return new Response(\\\"Daily limit reached\\\")
}

```

### CODE BLOCK 103
```

/admin/email

```

### CODE BLOCK 104
```

role = ADMIN

```

### CODE BLOCK 105
```
ts
import { prisma } from \\\"@/lib/prisma\\\"

export async function getEmailStats() {

  const totalSent = await prisma.emailQueue.count({
    where: { status: \\\"SENT\\\" }
  })

  const failed = await prisma.emailQueue.count({
    where: { status: \\\"FAILED\\\" }
  })

  const suppressed = await prisma.emailQueue.count({
    where: { status: \\\"SUPPRESSED\\\" }
  })

  const bounceEvents = await prisma.emailLog.count({
    where: { event: \\\"bounce\\\" }
  })

  const spamReports = await prisma.emailLog.count({
    where: { event: \\\"spamreport\\\" }
  })

  const unsubscribe = await prisma.emailLog.count({
    where: { event: \\\"unsubscribe\\\" }
  })

  const bounceRate = totalSent > 0
    ? (bounceEvents / totalSent) * 100
    : 0

  const complaintRate = totalSent > 0
    ? (spamReports / totalSent) * 100
    : 0

  return {
    totalSent,
    failed,
    suppressed,
    bounceRate,
    complaintRate,
    unsubscribe
  }
}

```

### CODE BLOCK 106
```
tsx
import { getEmailStats } from \\\"@/lib/emailAnalytics\\\"

export default async function EmailDashboard() {

  const stats = await getEmailStats()

  return (
    <div className=\\\"p-8\\\">
      <h1 className=\\\"text-2xl font-bold mb-6\\\">
        Email Infrastructure Dashboard
      </h1>

      <div className=\\\"grid grid-cols-3 gap-6\\\">

        <Card title=\\\"Total Sent\\\" value={stats.totalSent} />
        <Card title=\\\"Failed\\\" value={stats.failed} />
        <Card title=\\\"Suppressed\\\" value={stats.suppressed} />

        <Card title=\\\"Bounce Rate\\\" value={`${stats.bounceRate.toFixed(2)}%`} />
        <Card title=\\\"Complaint Rate\\\" value={`${stats.complaintRate.toFixed(2)}%`} />
        <Card title=\\\"Unsubscribes\\\" value={stats.unsubscribe} />

      </div>
    </div>
  )
}

function Card({ title, value }: any) {
  return (
    <div className=\\\"bg-white shadow rounded p-6\\\">
      <p className=\\\"text-gray-500\\\">{title}</p>
      <p className=\\\"text-2xl font-bold\\\">{value}</p>
    </div>
  )
}

```

### CODE BLOCK 107
```
ts
export function shouldPauseMarketing({
  bounceRate,
  complaintRate
}: {
  bounceRate: number
  complaintRate: number
}) {

  if (bounceRate > 5) return true
  if (complaintRate > 0.3) return true

  return false
}

```

### CODE BLOCK 108
```
ts
if (email.type === \\\"MARKETING\\\" && safetyTriggered) {
  continue
}

```

### CODE BLOCK 109
```

npm install recharts

```

### CODE BLOCK 110
```
ts
groupBy createdAt by date

```

### CODE BLOCK 111
```
bash
npm install

```

### CODE BLOCK 112
```
bash
npm run dev

```

### CODE BLOCK 113
```

ready - started server on 0.0.0.0:3000

```

### CODE BLOCK 114
```

http://localhost:3000

```

### CODE BLOCK 115
```

http://localhost:3000/admin/email

```

### CODE BLOCK 116
```

POST http://localhost:3000/api/send

```

### CODE BLOCK 117
```
json
{
  \\\"to\\\": \\\"yourtestemail@gmail.com\\\",
  \\\"subject\\\": \\\"Autoswift Test Email\\\",
  \\\"html\\\": \\\"<h1>Hello from Autoswift</h1>\\\"
}

```

### CODE BLOCK 118
```

http://localhost:3000/api/webhooks/sendgrid

```

### CODE BLOCK 119
```

autoswift-mailer/
│
├── app/
│   ├── admin/email/page.tsx
│   ├── api/send/route.ts
│   └── api/webhooks/sendgrid/route.ts
│
├── lib/
│   ├── prisma.ts
│   ├── emailWorker.ts
│   ├── emailAnalytics.ts
│   └── emailSafety.ts
│
├── prisma/
│   └── schema.prisma
│
├── workers/
│   └── mailQueue.ts
│
└── .env

```

### CODE BLOCK 120
```

DATABASE_URL=postgresql://postgres:password@localhost:5432/autoswift

SENDGRID_API_KEY=your_sendgrid_key

STRIPE_SECRET_KEY=your_stripe_key

NEXTAUTH_SECRET=randomsecret

NEXTAUTH_URL=http://localhost:3000

```

### CODE BLOCK 121
```
bash
npm install -g ngrok

```

### CODE BLOCK 122
```
bash
ngrok http 3000

```

### CODE BLOCK 123
```

https://a7f2-102-89-45-221.ngrok-free.app

```

### CODE BLOCK 124
```

https://xxxx.ngrok-free.app/admin/email

```

### CODE BLOCK 125
```
bash
npx create-next-app@latest autoswift-mailer

```

### CODE BLOCK 126
```
bash
cd autoswift-mailer

```

### CODE BLOCK 127
```
bash
npm install prisma @prisma/client sendgrid bullmq redis stripe next-auth recharts

```

### CODE BLOCK 128
```
bash
npm install -D prisma

```

### CODE BLOCK 129
```
bash
npx prisma init

```

### CODE BLOCK 130
```

prisma/schema.prisma
.env

```

### CODE BLOCK 131
```
prisma
generator client {
 provider = \\\"prisma-client-js\\\"
}

datasource db {
 provider = \\\"postgresql\\\"
 url      = env(\\\"DATABASE_URL\\\")
}

model User {
 id        String   @id @default(uuid())
 email     String   @unique
 password  String
 role      String   @default(\\\"USER\\\")
 createdAt DateTime @default(now())
}

model EmailQueue {
 id        String   @id @default(uuid())
 to        String
 subject   String
 html      String
 status    String   @default(\\\"PENDING\\\")
 type      String
 createdAt DateTime @default(now())
}

model EmailLog {
 id        String   @id @default(uuid())
 emailId   String
 event     String
 createdAt DateTime @default(now())
}

model SuppressionList {
 id        String   @id @default(uuid())
 email     String   @unique
 reason    String
 createdAt DateTime @default(now())
}

```

### CODE BLOCK 132
```
bash
npx prisma generate
npx prisma db push

```

### CODE BLOCK 133
```
env
DATABASE_URL=\\\"postgresql://postgres:password@localhost:5432/autoswift\\\"

SENDGRID_API_KEY=your_sendgrid_key

STRIPE_SECRET_KEY=your_stripe_key

NEXTAUTH_SECRET=autoswiftsecret

NEXTAUTH_URL=http://localhost:3000

```

### CODE BLOCK 134
```

/lib

```

### CODE BLOCK 135
```
ts
import { PrismaClient } from \\\"@prisma/client\\\"

export const prisma = new PrismaClient()

```

### CODE BLOCK 136
```

app/api/send

```

### CODE BLOCK 137
```
ts
import { prisma } from \\\"@/lib/prisma\\\"
import { NextResponse } from \\\"next/server\\\"

export async function POST(req: Request) {

  const body = await req.json()

  const email = await prisma.emailQueue.create({
    data: {
      to: body.to,
      subject: body.subject,
      html: body.html,
      type: \\\"TRANSACTIONAL\\\"
    }
  })

  return NextResponse.json({
    success: true,
    email
  })
}

```

### CODE BLOCK 138
```
bash
curl -X POST http://localhost:3000/api/send 
-H \\\"Content-Type: application/json\\\" 
-d '{\\\"to\\\":\\\"test@gmail.com\\\",\\\"subject\\\":\\\"Autoswift Test\\\",\\\"html\\\":\\\"Hello\\\"}'

```

### CODE BLOCK 139
```
bash
npx ngrok http 3000

```

### CODE BLOCK 140
```

https://8a21-102-89-44-11.ngrok-free.app

```

### CODE BLOCK 141
```

https://autoswift-mailer.vercel.app

```

### CODE BLOCK 142
```

autoswift-mailer
│
├── app
│   ├── page.tsx
│   ├── layout.tsx
│   │
│   ├── dashboard
│   │   └── page.tsx
│   │
│   ├── admin
│   │   └── email
│   │        └── page.tsx
│   │
│   └── api
│       ├── send
│       │    └── route.ts
│       │
│       └── webhooks
│            └── sendgrid
│                 └── route.ts
│
├── lib
│   ├── prisma.ts
│   ├── sendEmail.ts
│   ├── emailAnalytics.ts
│   └── auth.ts
│
├── prisma
│   └── schema.prisma
│
├── components
│   └── Card.tsx
│
├── .env
├── package.json
└── tsconfig.json

```

### CODE BLOCK 143
```
json
{
 \\\"name\\\": \\\"autoswift-mailer\\\",
 \\\"private\\\": true,
 \\\"scripts\\\": {
  \\\"dev\\\": \\\"next dev\\\",
  \\\"build\\\": \\\"next build\\\",
  \\\"start\\\": \\\"next start\\\"
 },
 \\\"dependencies\\\": {
  \\\"next\\\": \\\"14.2.0\\\",
  \\\"react\\\": \\\"18.2.0\\\",
  \\\"react-dom\\\": \\\"18.2.0\\\",
  \\\"@prisma/client\\\": \\\"^5.0.0\\\",
  \\\"prisma\\\": \\\"^5.0.0\\\",
  \\\"@sendgrid/mail\\\": \\\"^8.1.0\\\",
  \\\"next-auth\\\": \\\"^4.24.0\\\",
  \\\"stripe\\\": \\\"^14.0.0\\\",
  \\\"recharts\\\": \\\"^2.8.0\\\"
 }
}

```

### CODE BLOCK 144
```
ts
import { PrismaClient } from \\\"@prisma/client\\\"

const globalForPrisma = global as unknown as { prisma: PrismaClient }

export const prisma =
 globalForPrisma.prisma ||
 new PrismaClient({
  log: [\\\"query\\\"]
 })

if (process.env.NODE_ENV !== \\\"production\\\")
 globalForPrisma.prisma = prisma

```

### CODE BLOCK 145
```
ts
import sgMail from \\\"@sendgrid/mail\\\"

sgMail.setApiKey(process.env.SENDGRID_API_KEY!)

export async function sendEmail({ to, subject, html }: any) {

 await sgMail.send({
  to,
  from: \\\"noreply@autoswift.ai\\\",
  subject,
  html
 })

}

```

### CODE BLOCK 146
```
ts
import { prisma } from \\\"@/lib/prisma\\\"
import { sendEmail } from \\\"@/lib/sendEmail\\\"
import { NextResponse } from \\\"next/server\\\"

export async function POST(req: Request) {

 const body = await req.json()

 const email = await prisma.emailQueue.create({
  data: {
   to: body.to,
   subject: body.subject,
   html: body.html,
   type: \\\"TRANSACTIONAL\\\"
  }
 })

 await sendEmail(body)

 return NextResponse.json({
  success: true,
  email
 })
}

```

### CODE BLOCK 147
```
ts
import { prisma } from \\\"@/lib/prisma\\\"

export async function POST(req: Request) {

 const events = await req.json()

 for (const event of events) {

  await prisma.emailLog.create({
   data: {
    emailId: event.sg_message_id || \\\"unknown\\\",
    event: event.event
   }
  })

 }

 return new Response(\\\"ok\\\")
}

```

### CODE BLOCK 148
```
ts
import { prisma } from \\\"./prisma\\\"

export async function getEmailStats() {

 const totalSent = await prisma.emailQueue.count()

 const bounces = await prisma.emailLog.count({
  where: { event: \\\"bounce\\\" }
 })

 const complaints = await prisma.emailLog.count({
  where: { event: \\\"spamreport\\\" }
 })

 const bounceRate = totalSent > 0 ? (bounces / totalSent) * 100 : 0

 const complaintRate = totalSent > 0
  ? (complaints / totalSent) * 100
  : 0

 return {
  totalSent,
  bounceRate,
  complaintRate
 }

}

```

### CODE BLOCK 149
```
tsx
import { getEmailStats } from \\\"@/lib/emailAnalytics\\\"

export default async function Page() {

 const stats = await getEmailStats()

 return (

  <div className=\\\"p-10\\\">

   <h1 className=\\\"text-2xl font-bold mb-6\\\">
    Autoswift Email Dashboard
   </h1>

   <div className=\\\"grid grid-cols-3 gap-6\\\">

    <div className=\\\"p-6 bg-white shadow rounded\\\">
     <p>Total Sent</p>
     <h2>{stats.totalSent}</h2>
    </div>

    <div className=\\\"p-6 bg-white shadow rounded\\\">
     <p>Bounce Rate</p>
     <h2>{stats.bounceRate.toFixed(2)}%</h2>
    </div>

    <div className=\\\"p-6 bg-white shadow rounded\\\">
     <p>Complaint Rate</p>
     <h2>{stats.complaintRate.toFixed(2)}%</h2>
    </div>

   </div>

  </div>

 )

}

```

### CODE BLOCK 150
```

DATABASE_URL=postgresql://postgres:password@localhost:5432/autoswift

SENDGRID_API_KEY=your_sendgrid_key

NEXTAUTH_SECRET=autoswiftsecret

STRIPE_SECRET_KEY=your_stripe_key

```

### CODE BLOCK 151
```
bash
npx prisma db push

```

### CODE BLOCK 152
```
bash
git init
git add .
git commit -m \\\"autoswift mailer\\\"
git branch -M main
git remote add origin https://github.com/YOURNAME/autoswift-mailer.git
git push -u origin main

```

### CODE BLOCK 153
```

Cloudflare CDN
        │
        │
Load Balancer
        │
        │
Autoswift API Servers (Next.js)

```

### CODE BLOCK 154
```

                     INTERNET
                        │
                Cloudflare Edge
                        │
                 Load Balancer
                        │
         ┌─────────────────────────┐
         │ Autoswift API Cluster   │
         │ (Next.js SaaS Platform) │
         └─────────────────────────┘
              │            │
              │            │
        Redis Queue     PostgreSQL
              │            │
              │            │
        Email Worker Cluster
              │
              │
          SendGrid API
              │
              │
           Recipient

```

### CODE BLOCK 155
```

autoswift-platform
│
├── apps
│   ├── web
│   │   ├── app
│   │   ├── components
│   │   └── pages
│   │
│   └── api
│       ├── auth
│       ├── email
│       └── billing
│
├── services
│   ├── email-worker
│   ├── queue-service
│   ├── analytics-service
│   └── campaign-engine
│
├── infrastructure
│   ├── docker
│   ├── kubernetes
│   └── terraform
│
├── packages
│   ├── database
│   ├── auth
│   ├── email
│   └── analytics
│
├── prisma
│   └── schema.prisma
│
└── scripts

```

### CODE BLOCK 156
```

User
----
id
email
password
role
organizationId
createdAt

```

### CODE BLOCK 157
```

Organization
-------------
id
name
plan
createdAt

```

### CODE BLOCK 158
```

Campaign
---------
id
organizationId
name
type
status
createdAt

```

### CODE BLOCK 159
```

EmailJob
---------
id
campaignId
recipient
status
retryCount
createdAt

```

### CODE BLOCK 160
```

API → Queue → Worker → SendGrid

```

### CODE BLOCK 161
```

                Internet
                    │
            Cloudflare Edge CDN
                    │
             Global Load Balancer
                    │
        ┌─────────────────────────┐
        │     API Gateway         │
        │ (Autoswift Core API)   │
        └─────────────────────────┘
           │          │          │
           │          │          │
        Auth      Email API    Billing
           │          │          │
           │          │          │
        Redis Queue ────────────┘
              │
      Email Worker Cluster
              │
        Delivery Providers
              │
      Recipients Worldwide

```

### CODE BLOCK 162
```

autoswift-platform
│
├── apps
│   ├── web-app
│   │   ├── app
│   │   ├── components
│   │   └── dashboard
│   │
│   ├── api-server
│   │   ├── auth
│   │   ├── email
│   │   ├── campaigns
│   │   └── billing
│
├── services
│   ├── email-worker
│   ├── campaign-engine
│   ├── analytics-engine
│   └── webhook-listener
│
├── packages
│   ├── database
│   ├── auth
│   ├── email-core
│   └── queue
│
├── infrastructure
│   ├── docker
│   ├── kubernetes
│   └── terraform
│
├── prisma
│   └── schema.prisma
│
└── scripts

```

### CODE BLOCK 163
```

Organization
------------
id
name
plan
status
createdAt

```

### CODE BLOCK 164
```

User
-----
id
organizationId
email
password
role
createdAt

```

### CODE BLOCK 165
```

Campaign
--------
id
organizationId
name
type
status
createdAt

```

### CODE BLOCK 166
```

EmailJob
--------
id
campaignId
recipient
status
retryCount
createdAt

```

### CODE BLOCK 167
```

Suppression
-----------
email
reason
createdAt

```

### CODE BLOCK 168
```

Client → API → Queue → Worker → Provider

```

### CODE BLOCK 169
```

PENDING
PROCESSING
SENT
FAILED
RETRY
SUPPRESSED

```

### CODE BLOCK 170
```

SPF
DKIM
DMARC

```

### CODE BLOCK 171
```
id=\\\"autoswift-flow\\\"
User → CDN → Load Balancer → Autoswift API
       │                       │
       │                       ├── Auth Service
       │                       ├── Campaign Service
       │                       ├── Billing Service
       │                       └── Email Queue
       │
       └→ Redis Queue → Worker Cluster → Email Provider
                                          │
                                          └→ Recipients Worldwide

```

### CODE BLOCK 172
```

autoswift-platform
│
├── apps
│   ├── web-dashboard
│   └── api-server
│
├── services
│   ├── email-worker
│   ├── campaign-engine
│   ├── analytics-engine
│   └── webhook-listener
│
├── packages
│   ├── auth
│   ├── database
│   ├── email-core
│   └── queue
│
├── infrastructure
│   ├── docker
│   ├── kubernetes
│   └── terraform
│
├── prisma
│   └── schema.prisma
│
└── scripts

```

### CODE BLOCK 173
```

Organization
-------------
id
name
plan
status
createdAt

```

### CODE BLOCK 174
```

User
-----
id
organizationId
email
passwordHash
role
createdAt

```

### CODE BLOCK 175
```

Suppression
------------
email
reason
createdAt

```

### CODE BLOCK 176
```

API → Queue → Worker → Provider

```

### CODE BLOCK 177
```

Edge Layer
Application Layer
Messaging Layer
Data Layer
Delivery Layer
Monitoring Layer

```

### CODE BLOCK 178
```

Internet
   ↓
Cloudflare
   ↓
Load Balancer
   ↓
Autoswift API Cluster

```

### CODE BLOCK 179
```

Auth Service
Campaign Service
Email API
Billing Service
Admin Dashboard

```

### CODE BLOCK 180
```

User Request
   ↓
API
   ↓
Redis Queue
   ↓
Email Worker
   ↓
Provider API

```

### CODE BLOCK 181
```

PostgreSQL

```

### CODE BLOCK 182
```

Organizations
Users
Campaigns
EmailJobs
EmailEvents
SuppressionList
BillingSubscriptions

```

### CODE BLOCK 183
```

SENT
DELIVERED
BOUNCE
OPEN
CLICK
SPAM_REPORT
UNSUBSCRIBE

```

### CODE BLOCK 184
```

subscription plans
usage limits
invoice generation
payment webhooks
account suspension for overuse

```

### CODE BLOCK 185
```

bounce suppression
complaint suppression
unsubscribe handling
IP warmup

```

### CODE BLOCK 186
```

queue backlog
API errors
high bounce rate
delivery failures
database latency

```

### CODE BLOCK 187
```

autoswift-platform

```

### CODE BLOCK 188
```
id=\\\"repo-tree\\\"
autoswift-platform
│
├── apps
│   ├── web-dashboard
│   │   ├── app
│   │   │   ├── layout.tsx
│   │   │   ├── page.tsx
│   │   │   └── dashboard/page.tsx
│   │   └── components
│   │
│   └── api-server
│       ├── src
│       │   ├── controllers
│       │   ├── services
│       │   ├── routes
│       │   └── server.ts
│
├── services
│   ├── email-worker
│   │   └── worker.ts
│   │
│   ├── analytics-engine
│   │   └── analytics.ts
│   │
│   └── webhook-listener
│       └── sendgridWebhook.ts
│
├── packages
│   ├── database
│   │   └── prismaClient.ts
│   │
│   ├── auth
│   │   └── authService.ts
│   │
│   ├── email-core
│   │   └── mailer.ts
│   │
│   └── queue
│       └── queue.ts
│
├── prisma
│   └── schema.prisma
│
├── infrastructure
│   ├── docker
│   │   └── Dockerfile
│   │
│   ├── kubernetes
│   │   └── deployment.yaml
│   │
│   └── terraform
│       └── infrastructure.tf
│
├── scripts
│   └── start-workers.sh
│
└── .env.example

```

### CODE BLOCK 189
```
prisma id=\\\"schema\\\"
generator client {
 provider = \\\"prisma-client-js\\\"
}

datasource db {
 provider = \\\"postgresql\\\"
 url = env(\\\"DATABASE_URL\\\")
}

model Organization {
 id        String   @id @default(uuid())
 name      String
 plan      String
 createdAt DateTime @default(now())
 users     User[]
}

model User {
 id             String @id @default(uuid())
 organizationId String
 email          String @unique
 passwordHash   String
 role           String
 createdAt      DateTime @default(now())

 organization Organization @relation(fields:[organizationId], references:[id])
}

model Campaign {
 id             String @id @default(uuid())
 organizationId String
 name           String
 status         String
 createdAt      DateTime @default(now())
}

model EmailJob {
 id        String @id @default(uuid())
 recipient String
 subject   String
 html      String
 status    String
 retry     Int
 createdAt DateTime @default(now())
}

model EmailEvent {
 id        String @id @default(uuid())
 emailId   String
 event     String
 createdAt DateTime @default(now())
}

```

### CODE BLOCK 190
```
ts id=\\\"queue\\\"
import { Queue } from \\\"bullmq\\\"
import IORedis from \\\"ioredis\\\"

const connection = new IORedis(process.env.REDIS_URL!)

export const emailQueue = new Queue(\\\"emailQueue\\\", { connection })

```

### CODE BLOCK 191
```
ts id=\\\"mailer\\\"
import sgMail from \\\"@sendgrid/mail\\\"

sgMail.setApiKey(process.env.SENDGRID_API_KEY!)

export async function sendEmail(data:any) {

 await sgMail.send({
  to: data.to,
  from: \\\"noreply@autoswift.ai\\\",
  subject: data.subject,
  html: data.html
 })

}

```

### CODE BLOCK 192
```
ts id=\\\"worker\\\"
import { Worker } from \\\"bullmq\\\"
import { sendEmail } from \\\"../../packages/email-core/mailer\\\"

new Worker(\\\"emailQueue\\\", async job => {

 const data = job.data

 await sendEmail(data)

})

```

### CODE BLOCK 193
```
ts id=\\\"analytics\\\"
export function calculateRates(sent:number,bounce:number,complaint:number){

 const bounceRate = sent > 0 ? (bounce / sent) * 100 : 0
 const complaintRate = sent > 0 ? (complaint / sent) * 100 : 0

 return {
  bounceRate,
  complaintRate
 }

}

```

### CODE BLOCK 194
```
ts id=\\\"auth\\\"
import bcrypt from \\\"bcrypt\\\"

export async function hashPassword(password:string){
 return bcrypt.hash(password,10)
}

export async function verifyPassword(password:string,hash:string){
 return bcrypt.compare(password,hash)
}

```

### CODE BLOCK 195
```
dockerfile id=\\\"docker\\\"
FROM node:20

WORKDIR /app

COPY . .

RUN npm install

RUN npm run build

CMD [\\\"npm\\\",\\\"start\\\"]

```

### CODE BLOCK 196
```
yaml id=\\\"k8s\\\"
apiVersion: apps/v1
kind: Deployment
metadata:
 name: autoswift-api
spec:
 replicas: 3
 selector:
  matchLabels:
   app: autoswift
 template:
  metadata:
   labels:
    app: autoswift
  spec:
   containers:
   - name: autoswift
     image: autoswift/api
     ports:
      - containerPort: 3000

```

### CODE BLOCK 197
```

Internet
↓
CDN / Firewall
↓
Load Balancer
↓
Autoswift API

```

### CODE BLOCK 198
```

Autoswift global email delivery SaaS platform

```

### CODE BLOCK 199
```
bash
git clone https://github.com/YOURNAME/autoswift-platform.git

```

### CODE BLOCK 200
```
bash
cd autoswift-platform

```

### CODE BLOCK 201
```
bash
npm init -y

```

### CODE BLOCK 202
```
bash
npm install next react react-dom prisma @prisma/client bullmq ioredis stripe bcrypt

```

### CODE BLOCK 203
```
bash
npm install -D typescript ts-node nodemon

```

### CODE BLOCK 204
```

DATABASE_URL=postgresql://postgres:password@localhost:5432/autoswift
REDIS_URL=redis://localhost:6379
SENDGRID_API_KEY=your_api_key
STRIPE_SECRET_KEY=your_stripe_key

```

### CODE BLOCK 205
```
bash
npx prisma generate

```

### CODE BLOCK 206
```
bash
redis-server

```

### CODE BLOCK 207
```
bash
node services/email-worker/worker.js

```

### CODE BLOCK 208
```

http://localhost:3000/admin

```

### CODE BLOCK 209
```

autoswift-platform
│
├── apps
│   ├── web
│   │   ├── app
│   │   │   ├── layout.tsx
│   │   │   ├── page.tsx
│   │   │   └── dashboard/page.tsx
│   │   └── package.json
│   │
│   └── api
│       ├── src
│       │   ├── server.ts
│       │   ├── routes
│       │   │   └── email.ts
│       │   └── services
│       │       └── emailService.ts
│       └── package.json
│
├── services
│   └── worker
│       └── worker.ts
│
├── packages
│   ├── database
│   │   └── prisma.ts
│   │
│   ├── queue
│   │   └── queue.ts
│   │
│   └── mailer
│       └── mailer.ts
│
├── prisma
│   └── schema.prisma
│
├── docker
│   └── Dockerfile
│
├── package.json
└── .env.example

```

### CODE BLOCK 210
```

{
  \\\"name\\\": \\\"autoswift-platform\\\",
  \\\"private\\\": true,
  \\\"workspaces\\\": [
    \\\"apps/*\\\",
    \\\"packages/*\\\",
    \\\"services/*\\\"
  ],
  \\\"scripts\\\": {
    \\\"dev:web\\\": \\\"npm run dev --workspace web\\\",
    \\\"dev:api\\\": \\\"npm run dev --workspace api\\\",
    \\\"worker\\\": \\\"ts-node services/worker/worker.ts\\\"
  }
}

```

### CODE BLOCK 211
```

DATABASE_URL=postgresql://postgres:password@localhost:5432/autoswift
REDIS_URL=redis://localhost:6379
SENDGRID_API_KEY=your_sendgrid_key
FROM_EMAIL=noreply@autoswift.ai
STRIPE_SECRET_KEY=your_stripe_key

```

### CODE BLOCK 212
```

generator client {
 provider = \\\"prisma-client-js\\\"
}

datasource db {
 provider = \\\"postgresql\\\"
 url = env(\\\"DATABASE_URL\\\")
}

model Organization {
 id        String   @id @default(uuid())
 name      String
 createdAt DateTime @default(now())
 users     User[]
}

model User {
 id             String   @id @default(uuid())
 organizationId String
 email          String   @unique
 passwordHash   String
 role           String
 createdAt      DateTime @default(now())

 organization Organization @relation(fields:[organizationId], references:[id])
}

model EmailJob {
 id        String   @id @default(uuid())
 recipient String
 subject   String
 html      String
 status    String
 retry     Int      @default(0)
 createdAt DateTime @default(now())
}

model EmailEvent {
 id        String   @id @default(uuid())
 emailId   String
 event     String
 createdAt DateTime @default(now())
}

```

### CODE BLOCK 213
```

import { PrismaClient } from \\\"@prisma/client\\\"

export const prisma = new PrismaClient()

```

### CODE BLOCK 214
```

import { Queue } from \\\"bullmq\\\"
import IORedis from \\\"ioredis\\\"

const connection = new IORedis(process.env.REDIS_URL!)

export const emailQueue = new Queue(\\\"emailQueue\\\", {
 connection
})

```

### CODE BLOCK 215
```

import sgMail from \\\"@sendgrid/mail\\\"

sgMail.setApiKey(process.env.SENDGRID_API_KEY!)

export async function sendEmail(data:any){

 await sgMail.send({
  to: data.to,
  from: process.env.FROM_EMAIL!,
  subject: data.subject,
  html: data.html
 })

}

```

### CODE BLOCK 216
```

import express from \\\"express\\\"
import emailRoutes from \\\"./routes/email\\\"

const app = express()

app.use(express.json())

app.use(\\\"/email\\\", emailRoutes)

app.listen(4000, () => {
 console.log(\\\"API running on port 4000\\\")
})

```

### CODE BLOCK 217
```

import { Router } from \\\"express\\\"
import { emailQueue } from \\\"../../../packages/queue/queue\\\"

const router = Router()

router.post(\\\"/send\\\", async (req, res) => {

 const { to, subject, html } = req.body

 await emailQueue.add(\\\"sendEmail\\\", {
  to,
  subject,
  html
 })

 res.json({
  status: \\\"queued\\\"
 })

})

export default router

```

### CODE BLOCK 218
```

import { Worker } from \\\"bullmq\\\"
import { sendEmail } from \\\"../../packages/mailer/mailer\\\"

new Worker(\\\"emailQueue\\\", async job => {

 const { to, subject, html } = job.data

 await sendEmail({
  to,
  subject,
  html
 })

})

```

### CODE BLOCK 219
```

export default function Home() {
 return (
  <div>
   <h1>Autoswift Email Platform</h1>
   <p>Global email delivery infrastructure</p>
  </div>
 )
}

```

### CODE BLOCK 220
```

FROM node:20

WORKDIR /app

COPY . .

RUN npm install

RUN npm run build

CMD [\\\"npm\\\",\\\"start\\\"]

```

### CODE BLOCK 221
```

npm install

```

### CODE BLOCK 222
```

npm install express bullmq ioredis @sendgrid/mail prisma @prisma/client

```

### CODE BLOCK 223
```

npm run dev:api

```

### CODE BLOCK 224
```

npm run worker

```

### CODE BLOCK 225
```

npm run dev:web

```
